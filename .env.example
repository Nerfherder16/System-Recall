# =============================================================================
# PROJECT RECALL - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values

# -----------------------------------------------------------------------------
# Neo4j Graph Database
# -----------------------------------------------------------------------------
NEO4J_USER=neo4j
NEO4J_PASSWORD=change-me-in-production

# -----------------------------------------------------------------------------
# LLM Configuration (for fact extraction & embeddings)
# -----------------------------------------------------------------------------
# Provider: ollama, openai, anthropic
LLM_PROVIDER=ollama

# For Ollama (local inference):
LLM_BASE_URL=http://localhost:11434
LLM_MODEL=llama3.1:8b
EMBEDDING_MODEL=nomic-embed-text

# For OpenAI:
# LLM_PROVIDER=openai
# OPENAI_API_KEY=sk-your-key-here
# LLM_MODEL=gpt-4o
# EMBEDDING_MODEL=text-embedding-3-small

# For Anthropic:
# LLM_PROVIDER=anthropic
# ANTHROPIC_API_KEY=your-key-here
# LLM_MODEL=claude-sonnet-4-20250514

# -----------------------------------------------------------------------------
# Memory Scoping
# -----------------------------------------------------------------------------
# Default project ID for memories
DEFAULT_PROJECT_ID=default

# Default user ID (override per-session via MCP config)
DEFAULT_USER_ID=dev

# -----------------------------------------------------------------------------
# Signal Detection
# -----------------------------------------------------------------------------
# Keywords that trigger immediate memory capture (comma-separated)
SIGNAL_KEYWORDS=remember,decided,architecture,important,don't forget,note to self,bug fix,breaking change

# -----------------------------------------------------------------------------
# Logging
# -----------------------------------------------------------------------------
LOG_LEVEL=INFO
